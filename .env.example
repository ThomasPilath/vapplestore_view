# ============================================
# VAPPLESTORE VIEW - Configuration Template
# ============================================
# Copiez ce fichier en .env.local pour développement
# ou .env pour production
# ============================================

# ------------------------------
# 🗄️ DATABASE CONFIGURATION
# ------------------------------
# Configuration de connexion à la base de données MySQL/MariaDB

DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_NAME=vapplestore
DATABASE_USER=root
DATABASE_PASSWORD=your_mysql_password_here

# MariaDB container (Docker only)
MARIADB_ROOT_PASSWORD=your_root_password_here

# ------------------------------
# 🔐 JWT SECRETS
# ------------------------------
# ⚠️ IMPORTANT: Générez des secrets forts avant production!
# Commande: openssl rand -base64 64
# NE JAMAIS utiliser les valeurs par défaut en production!

JWT_ACCESS_SECRET= #Générez votre token avec : openssl rand -base64 64
JWT_REFRESH_SECRET= #Générez votre token avec : openssl rand -base64 64 (différent!)

# ------------------------------
# 👤 ADMIN INITIAL
# ------------------------------
# Utilisateur admin créé lors de l'initialisation (bun run init-prod)
# ⚠️ Changez le mot de passe après première connexion!

ADMIN_USERNAME=admin
ADMIN_PASSWORD=AdminPassword123

# ------------------------------
# 🌐 CORS CONFIGURATION
# ------------------------------
# Liste des origines autorisées (comma-separated)
# Développement: localhost + ports Docker
# Production: votre(vos) domaine(s)

ALLOWED_ORIGINS=http://localhost:3000,https://votre-domaine.com

# Exemples:
# Dev: ALLOWED_ORIGINS=http://localhost:3000
# Prod: ALLOWED_ORIGINS=https://votredomaine.com,https://www.votredomaine.com

# ------------------------------
# 🐳 DOCKER CONFIGURATION
# ------------------------------
# Ports d'exposition pour les conteneurs

APP_EXPOSITION_PORT=3000
PHPMYADMIN_PORT=8080

# ------------------------------
# 🐙 DOCKER HUB (CI/CD)
# ------------------------------
# Pour GitHub Actions - push images Docker

# DOCKERHUB_USERNAME=votre_username
# DOCKERHUB_TOKEN=votre_token

# ------------------------------
# 📊 MONITORING (Optionnel)
# ------------------------------
# Intégration Sentry pour tracking erreurs

# SENTRY_DSN=https://...@sentry.io/...
# SENTRY_ENVIRONMENT=production

# ------------------------------
# 🔧 API KEYS (Optionnel)
# ------------------------------
# Pour intégrations futures

# API_KEY=your_api_key_here

# ============================================
# 📚 DOCUMENTATION
# ============================================
# README.md - Guide complet d'installation et déploiement
# /api/db-check - Vérifier migrations
# /api/init - Initialiser base de données
# ============================================

